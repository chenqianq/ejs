/**
 * Created by Administrator on 2017/12/6.
 */
function openItem(parm) {
     //second-part
    $(".second-part ul").hide();
    var currentObj = $(".second-part ."+parm).eq(0);
    currentObj.show();
    //去除默认打开左侧第一个菜单
    // currentObj.find("dl a").eq(0).trigger("click");
}
